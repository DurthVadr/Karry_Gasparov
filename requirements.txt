# Core dependencies
chess==1.11.2          # Python chess library
torch>=2.0.0           # PyTorch for neural networks
numpy>=1.20.0          # Numerical computing

# GUI dependencies
Pillow==10.2.0         # Image processing
cairosvg==2.7.1        # SVG to PNG conversion
tkinter                # GUI framework (usually comes with Python)

# Training dependencies
matplotlib>=3.5.0      # For plotting training progress

# Optional dependencies
# stockfish              # Chess engine (install separately via package manager)
